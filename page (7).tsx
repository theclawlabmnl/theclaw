"use client";import{useState}from"react";import{useRouter}from"next/navigation";import{supabaseBrowser}from"@/lib/supabase-browser";export default function Login(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[err,setErr]=useState(""),router=useRouter();return <main className="form-page"><div className="container" style={{maxWidth:450}}><div className="card"><div className="kicker">Owner access</div><h1 className="serif">Admin login</h1><div className="field"><label>Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)}/></div><div className="field"><label>Password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></div>{err&&<div className="notice">{err}</div>}<button className="btn" style={{marginTop:16}} onClick={async()=>{const{error}=await supabaseBrowser().auth.signInWithPassword({email,password});if(error)setErr(error.message);else router.push("/admin")}}>Sign in</button></div></div></main>}