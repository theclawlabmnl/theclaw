'use client';import {useState} from "react";import {useRouter} from "next/navigation";export default function SubmitRequest({token}:{token:string}){const[r,setR]=useState(false);const router=useRouter();return <button className="btn" disabled={r} onClick={async()=>{setR(true);const x=await fetch(`/api/bookings?token=${token}`,{method:"PATCH"});const j=await x.json();if(x.ok)router.push(`/status/${token}`);else{alert(j.error||"Unable to submit");setR(false)}}}>{r?"Submitting…":"SUBMIT BOOKING REQUEST"}</button>}