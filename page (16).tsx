export const dynamic = "force-dynamic";
import {supabaseAdmin} from "@/lib/supabase-admin";import ReviewForm from "@/components/ReviewForm";
export default async function ReviewPage({params}:{params:Promise<{token:string}>}){const{token}=await params;const db=supabaseAdmin();const{data:b}=await db.from("bookings").select("id,customer_name,status").eq("access_token",token).single();if(!b||b.status!=="completed")return <main className="form-page"><div className="container card">Reviews become available after an appointment is marked completed.</div></main>;return <main className="form-page"><div className="container" style={{maxWidth:650}}><div className="kicker">The Claw Lab</div><h1 className="serif">How was your Claw Lab experience? ♡</h1><ReviewForm token={token} name={b.customer_name}/></div></main>}