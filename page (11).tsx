export const dynamic = "force-dynamic";
import {supabaseAdmin}from"@/lib/supabase-admin";import PaymentQueue from "@/components/PaymentQueue";export default async function Payments(){const db=supabaseAdmin();const{data}=await db.from("payments").select("*,bookings(reference_code,customer_name,preferred_date,preferred_time,access_token),payment_proofs(path)").order("created_at",{ascending:false});return <><div className="section-head"><div><div className="kicker">Money, manually</div><h1 className="serif">Payments</h1><p className="muted">No payment gateway. Review submitted proof here.</p></div></div><PaymentQueue payments={data||[]}/></>}