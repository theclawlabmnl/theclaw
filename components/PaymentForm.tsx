'use client';import {useState} from "react";import {useRouter} from "next/navigation";export default function PaymentForm({token,gcash,qrph}:{token:string,gcash:any,qrph:any}){const[method,setMethod]=useState("gcash"),[file,setFile]=useState<File|null>(null),[busy,setBusy]=useState(false);const router=useRouter();const qr=method==="gcash"?gcash.gcash_qr:qrph.qrph_qr;const amount=method==="gcash"?200:200+Number(qrph.qrph_fee||5);return <div><div className="inline-grid"><label className="card"><input type="radio" checked={method==="gcash"} onChange={()=>setMethod("gcash")}/> GCash — ₱200</label><label className="card"><input type="radio" checked={method==="qrph"} onChange={()=>setMethod("qrph")}/> QR PH — ₱{amount}</label></div><div className="card" style={{marginTop:15,textAlign:"center"}}>{qr?<img src={qr} alt={`${method} QR`} style={{maxWidth:"280px",width:"100%",height:"auto",borderRadius:12}}/>:<div className="notice">QR image not configured yet. Admin can upload it under Payment Settings.</div>}<p>Scan the QR code using your preferred banking/e-wallet app.</p></div><div className="field"><label>Proof of Payment</label><input type="file" accept=".jpg,.jpeg,.png,.heic,image/jpeg,image/png" onChange={e=>setFile(e.target.files?.[0]||null)}/></div><button className="btn" disabled={!file||busy} onClick={async()=>{if(!file)return;setBusy(true);const d=new FormData();d.append("token",token);d.append("method",method);d.append("amount",String(amount));d.append("proof",file);const r=await fetch("/api/payment-proof",{method:"POST",body:d});if(r.ok)router.push(`/status/${token}`);else alert((await r.json()).error||"Upload failed");setBusy(false)}}>{busy?"Submitting…":"SUBMIT PAYMENT PROOF"}</button></div>}