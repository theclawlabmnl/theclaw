"use client";import{useState}from"react";export default function AvailabilityManager({rules,overrides}:{rules:any[],overrides:any[]}){const[date,setDate]=useState(""),[start,setStart]=useState(""),[end,setEnd]=useState(""),[kind,setKind]=useState("open");const add=async()=>{const r=await fetch("/api/admin/availability",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({date,start,end,kind})});if(!r.ok)alert((await r.json()).error);else location.reload()};return <div className="grid"><div className="card"><h3>Semester schedule</h3>{rules.map(r=><p key={r.id}><strong>{["Sun","Mon","Tue","Wed","Thu","Fri","Sat"][r.day_of_week]}</strong> · {r.is_available?r.start_time+"–"+r.end_time:"School / unavailable"}</p>)}{!rules.length&&<p className="muted">No semester rules configured yet.</p>}</div><div className="card"><h3>OPEN EXTRA TIME / BLOCK TIME</h3><div className="field"><label>Date</label><input type="date" value={date} onChange={e=>setDate(e.target.value)}/></div><div className="inline-grid"><input type="time" value={start} onChange={e=>setStart(e.target.value)}/><input type="time" value={end} onChange={e=>setEnd(e.target.value)}/></div><div className="field"><select value={kind} onChange={e=>setKind(e.target.value)}><option value="open">OPEN EXTRA TIME</option><option value="block">BLOCK TIME</option></select></div><button className="btn" onClick={add}>Save override</button></div><div className="card"><h3>Overrides</h3>{overrides.map(o=><p key={o.id}><strong>{o.override_date}</strong> · {o.start_time}–{o.end_time} · {o.kind}</p>)}</div></div>}